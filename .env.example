# Copy this file to .env.local and fill in your project's values
# (Supabase Dashboard → Project Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Email that is auto-promoted to the platform-admin (superadmin) role on sign-in.
# Keep the real address here (gitignored) rather than in source code.
VITE_SUPERADMIN_EMAIL=

import type { Purchase, Sale } from '../types'

/** Fresh start: no seeded sales or purchases. */
export const SALES: Record<string, Sale[]> = {}

export const PURCHASES: Record<string, Purchase[]> = {}
